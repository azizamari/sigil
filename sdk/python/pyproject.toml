[project]
name = "sigil-client"
version = "0.1.0"
description = "Client for the sigil forensic watermarking API"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["sigil*"]
